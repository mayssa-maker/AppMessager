import 'svelte-bootstrap/dist/bootstrap.min.css';
import { Router } from 'svelte-routing';
import App from './App.svelte';

const app = new Router({
  routes
    // Add other routes as needed
  
});

export default app;